"""Generated gRPC stubs — created by Ansible xinas_menu role.

This package is populated at deployment time by:
  ansible-playbook playbooks/site.yml --tags xinas_menu

Do not edit manually.
"""
import os as _os
import sys as _sys

# protoc-generated _pb2.py files use bare sibling imports
# (e.g. `import message_config_pb2`). Add this directory to sys.path
# so those imports resolve regardless of how the package is loaded.
_proto_dir = _os.path.dirname(__file__)
if _proto_dir not in _sys.path:
    _sys.path.insert(0, _proto_dir)
