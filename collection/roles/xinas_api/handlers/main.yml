---
# xinas_api role handlers.

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart xinas-api
  ansible.builtin.service:
    name: xinas-api
    state: restarted
